"""Package containing factories used by the processors:

* Generic factory to manage the relationship between mapping keys and the corresponding
    value pairs of type<T> and type<R> respectively via :class: `~.generic_factory.GenericFactory`.
"""

from .generic_factory import GenericFactory
